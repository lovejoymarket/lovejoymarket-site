LOVEJOY INBOX LINK LAYOUT V4

Upload ONE file to the GitHub repo root:
- section-pages.css

Replace the existing section-pages.css.

WHAT CHANGES
1. The TOP dropdown is now intentionally simple:
   - one small hot-pink bar
   - Tattoo Menu / Events Menu / Market Menu / My LoveJoy Menu
   - simple open / close indicator
   - the opened dropdown is only a plain, readable list of page links
   - NO shimmer
   - NO fake inbox toolbar
   - NO random “messages” text

2. THE BIG LINKS ON THE MAIN HUB PAGES are now the MySpace inbox:
   - the square card grid is gone
   - each page choice is one stacked message row
   - navy inbox header
   - alternating cream / pale-pink rows
   - little LoveJoy sender line
   - pink subject/title
   - short preview text
   - small “open page” button
   - existing heart/star icons become the cute message/avatar column
   - mobile becomes one clean stacked inbox instead of two tiny square columns

3. This applies consistently to the current section hub cards on:
   - Tattoos
   - Events
   - Market
   - anywhere else using the same section-route-card system

4. About is NOT touched.
5. Games, surveys, forms, Starcade, HTML, styles.css, site.js, PDFs and images are NOT touched.

If Safari shows the previous version immediately after upload, close the tab and reopen lovejoymarket.co once so the replaced stylesheet is reloaded.
